/**
 * Created by atg on 28/01/2015.
 */
//Manage all the labels (sprites) for the website

function spriteManager() {
    var labels = [];

    create: function(name, position, scale, colour, fontSize, opacity, visible) {

    }
}